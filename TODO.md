# 精力配置洞察报告 - 前后端接口对接文档

> 本文档用于前端/模板与后端老师对接数据接口，确保数据结构对称。

---

## 一、接口清单

| 接口地址 | 方法 | 说明 | 状态 |
|---------|------|------|------|
| `/api/schedule/report/json` | GET | 获取报表原始 JSON 数据（调试用） | ✅ 已联调 |
| `/api/schedule/report/html` | GET | **调试接口**：浏览器直接预览 HTML | ✅ 已联调 |
| `/api/schedule/report/download` | GET | 生成并下载 PDF 报表 | ✅ 已联调 |

**前端调用方式**：
- PDF 下载：`<a href="http://localhost:8080/api/schedule/report/download" download>`
- JSON 查看：`<a href="http://localhost:8080/api/schedule/report/json" target="_blank">`
- HTML 预览（调试用）：`http://localhost:8080/api/schedule/report/html`

---

## 二、响应数据模型（ReportData）

后端返回的 JSON 必须严格匹配以下结构：

```json
{
  "title": "管理者精力配置洞察报告 – 2025年3月",
  "analyzedPerson": "张总监",
  "department": "销售部",
  "healthScore": 85,
  "精力总结": "当前精力投入中，外部商务类事务占比最高（约38.5%）...",
  "业务类别占比": [
    { "外部商务": 38.5 },
    { "技术讨论/研讨": 32.7 },
    { "其他": 28.8 }
  ],
  "职责对比": [
    { "name": "职责1", "actual": 100, "ideal": 150 },
    { "name": "职责2", "actual": 140, "ideal": 100 },
    { "name": "职责3", "actual": 230, "ideal": 200 },
    { "name": "职责4", "actual": 100, "ideal": 140 },
    { "name": "职责5", "actual": 130, "ideal": 100 }
  ],
  "问题诊断与风险预警": "1、外部商务事务过度挤占核心开发时间...",
  "精力平衡建议": "1、立即建立日程准入审核机制...",
  "disclaimer": "内容由AI生成，仅供参考"
}
```

---

## 三、字段详细说明

### 3.1 基础信息字段

| 字段名 | 类型 | 是否必填 | 说明 |
|-------|------|---------|------|
| `title` | String | 是 | 报告标题，如：管理者精力配置洞察报告 – 2025年3月 |
| `analyzedPerson` | String | 是 | 被分析人姓名，如：张总监 |
| `department` | String | 是 | 所属部门，如：销售部 |
| `healthScore` | int | 是 | 精力健康度评分（0-100） |

### 3.2 精力总结

| 字段名 | 类型 | 是否必填 | 说明 |
|-------|------|---------|------|
| `精力总结` | String | 是 | 纯文本总结，直接渲染在"一、精力健康度概览"区块 |

### 3.3 业务类别占比（环形图数据）

| 字段名 | 类型 | 是否必填 | 说明 |
|-------|------|---------|------|
| `业务类别占比` | `List<Map<String, Number>>` | 是 | 每个 Map **只有一个 Entry**：key 为类别名称，value 为百分比数值 |

**约束**：
- 每项必须是单 Entry Map，如 `{ "外部商务": 38.5 }`
- value 为数值类型（int 或 double），**不需要带 `%` 符号**
- 前端会将其转为 ECharts 饼图数据 `{ name: "外部商务", value: 38.5 }`

**示例**：
```json
[
  { "外部商务": 38.5 },
  { "技术讨论/研讨": 32.7 },
  { "其他": 28.8 }
]
```

### 3.4 职责对比（折线图数据）⚠️ 重点

| 字段名 | 类型 | 是否必填 | 说明 |
|-------|------|---------|------|
| `职责对比` | `List<Map<String, Object>>` | 是 | 每条数据必须包含 `name`、`actual`、`ideal` 三个字段 |

**约束**：
- `name`: String → X 轴职责名称（如：职责1、职责2...）
- `actual`: Number → 实际投入数值（Y 轴）
- `ideal`: Number → 理想状态数值（Y 轴）
- **三条数据必须一一对应**，顺序决定 X 轴从左到右的排列

**示例**：
```json
[
  { "name": "职责1", "actual": 100, "ideal": 150 },
  { "name": "职责2", "actual": 140, "ideal": 100 },
  { "name": "职责3", "actual": 230, "ideal": 200 },
  { "name": "职责4", "actual": 100, "ideal": 140 },
  { "name": "职责5", "actual": 130, "ideal": 100 }
]
```

> ⚠️ **注意**：之前拆分为 `实际投入` 和 `理想状态` 两个独立列表的方案已废弃，因为 key 无法对齐，无法绘制对比折线图。请后端统一按 `职责对比` 格式返回。

### 3.5 问题诊断与精力平衡建议

| 字段名 | 类型 | 是否必填 | 说明 |
|-------|------|---------|------|
| `问题诊断与风险预警` | String | 是 | 纯文本，支持用 `1、` `2、` 等手动编号，直接渲染在"三、问题诊断与风险预警"区块 |
| `精力平衡建议` | String | 是 | 纯文本，支持用 `1、` `2、` 等手动编号，直接渲染在"四、精力平衡建议"区块 |

---

## 四、PDF 报表模板结构

后端生成 PDF 时，Thymeleaf 模板 `report.html` 的章节结构如下：

| 章节 | 标题 | 数据来源 |
|------|------|---------|
| 头部 | 标题 + 被分析人 + 部门 | `title` / `analyzedPerson` / `department` |
| 一 | 精力健康度概览 | `healthScore` + `精力总结` |
| 二 | 时间分配与职责匹配度分析 | `业务类别占比`（环形图）+ `职责对比`（折线图）|
| 三 | 问题诊断与风险预警 | `问题诊断与风险预警` |
| 四 | 精力平衡建议 | `精力平衡建议` |
| 底部 | 免责声明 | 模板写死："内容由AI生成，仅供参考" |

---

## 五、当前状态与待办

### 5.1 已完成（前端/模板侧）

- [x] DTO `ReportData` 定义完成（适配真实数据结构）
- [x] Thymeleaf 模板 `report.html` 适配真实数据结构
- [x] ECharts 环形图（业务类别占比）渲染逻辑，无 label、无 legend、居中
- [x] ECharts 折线图（职责对比）渲染逻辑，关闭动画
- [x] 问题诊断 / 精力平衡建议 纯文本渲染
- [x] Playwright PDF 生成链路打通，固定视口 1400x900
- [x] ECharts 动画关闭（`animation: false`），避免 PDF 截图截到动画中途
- [x] `GET /api/schedule/report/json` 接口（返回原始 JSON）
- [x] `GET /api/schedule/report/html` 调试接口（浏览器直接预览）
- [x] `GET /api/schedule/report/download` 接口（返回 PDF）
- [x] `demo.html` 添加"查看原始 JSON 数据"按钮
- [x] 移除 Lombok（Java 25 兼容性问题）
- [x] 删除不再使用的旧 DTO（BusinessItem、DutyComparison、ProblemItem、SuggestionItem、SalesRecord）

### 5.2 待后端老师提供

#### 真实数据结构现状

后端目前提供的数据格式如下（**缺少关键字段，且折线图数据结构不匹配**）：

```json
{
  "精力总结": "...",
  "业务类别占比": [
    { "外部商务": 38.5 },
    { "技术讨论/研讨": 32.7 },
    { "其他": 28.8 }
  ],
  "实际投入": [
    { "项目推进": 15.4 },
    { "团队协作": 17.3 },
    { "外部商务": 38.5 },
    { "其他": 28.8 }
  ],
  "理想状态": [
    { "项目推进": 40.0 },
    { "团队协作": 25.0 },
    { "代码质量保障": 15.0 },
    { "系统稳定性优化": 10.0 },
    { "学习成长": 10.0 }
  ],
  "问题诊断与风险预警": "...",
  "精力平衡建议": "..."
}
```

#### ⚠️ 缺失字段（必须补）

| 缺失字段 | 类型 | 说明 |
|---------|------|------|
| `title` | String | 报告标题，如：管理者精力配置洞察报告 – 2025年3月 |
| `analyzedPerson` | String | 被分析人姓名，如：张总监 |
| `department` | String | 所属部门，如：销售部 |
| `healthScore` | int | 精力健康度评分（0-100），目前完全缺失 |
| `职责对比` | `List<Map>` | **折线图数据源，见下方详细说明** |
| `disclaimer` | String | 免责声明，如：内容由AI生成，仅供参考 |

#### ⚠️ 数据结构不匹配（必须改）

**问题 1：折线图数据格式不匹配**

后端当前提供的是两个独立列表：
- `实际投入`：[{项目推进:15.4}, {团队协作:17.3}, {外部商务:38.5}, {其他:28.8}]
- `理想状态`：[{项目推进:40.0}, {团队协作:25.0}, {代码质量保障:15.0}, {系统稳定性优化:10.0}, {学习成长:10.0}]

**问题**：两者的 key 不一致（实际投入 4 项 vs 理想状态 5 项，且"外部商务/其他" vs "代码质量保障/系统稳定性优化/学习成长"），无法直接合并为对比折线图。

**要求**：后端提供一个统一的 `职责对比` 字段，格式如下：

```json
"职责对比": [
  { "name": "项目推进", "actual": 15.4, "ideal": 40.0 },
  { "name": "团队协作", "actual": 17.3, "ideal": 25.0 },
  { "name": "外部商务", "actual": 38.5, "ideal": 0 },
  { "name": "代码质量保障", "actual": 0, "ideal": 15.0 },
  { "name": "系统稳定性优化", "actual": 0, "ideal": 10.0 },
  { "name": "学习成长", "actual": 0, "ideal": 10.0 }
]
```

> 如果某类事务在实际投入或理想状态中有一方没有数据，对应的值填 **0**。

**问题 2：业务类别占比和实际投入/理想状态的数据关系**

- `业务类别占比`：外部商务(38.5%) + 技术讨论/研讨(32.7%) + 其他(28.8%) = **100%**
- `实际投入`：项目推进(15.4) + 团队协作(17.3) + 外部商务(38.5) + 其他(28.8) = **100%**

两者的数值和分类方式不同（一个是业务类别视角，一个是项目/职责视角）。前端目前是**分别独立展示**的：
- 环形图 = `业务类别占比`
- 折线图 = `职责对比`

请后端确认：
1. 这两组数据是否独立生成？还是可以从同一数据源派生？
2. `业务类别占比` 的分类维度（外部商务 / 技术讨论/研讨 / 其他）是否固定？

#### 待确认事项

- [ ] **真实数据接入**：替换 `ReportService.generateMockData()` 中的 mock 数据
- [ ] **缺失字段补全**：title、analyzedPerson、department、healthScore、disclaimer
- [ ] **职责对比字段**：按 `{name, actual, ideal}` 格式提供，缺失项填 0
- [ ] **healthScore 算法**：健康评分的计算逻辑（目前是 mock 值 85）
- [ ] **精力总结 / 问题诊断 / 建议**：确认是否由 AI 生成后直接入库，还是需要后端做额外格式化

### 5.3 注意事项

1. **中文字段名**：DTO 中使用了中文字段名（如 `精力总结`、`业务类别占比`），Jackson 序列化时会原样输出。如果后端不习惯中文字段名，可以改成英文驼峰，但需同步修改前端模板中的 `${data.xxx}` 引用。

2. **业务类别占比格式**：必须是 `List<Map<String, Number>>`，每个 Map 仅含一个 Entry。如果后端觉得这种格式别扭，可以改成 `List<BusinessCategory>` 对象列表，字段为 `name` + `value`，前端同步调整 JS 解析逻辑即可。

3. **职责对比格式**：强烈建议保持 `{name, actual, ideal}` 的对象数组形式，这是画对比折线图最直观的结构。

4. **PDF 与浏览器预览一致性**：`ReportService.java` 中已固定 `page.setViewportSize(1400, 900)`，确保 PDF 渲染有稳定的宽度基准。浏览器预览时窗口宽度可能不同，布局会有差异，这是正常现象。以 PDF 输出为准。

---

## 六、联调检查清单

后端完成真实数据接入后，请按以下步骤验证：

- [ ] 启动服务，访问 `http://localhost:8080/api/schedule/report/json`
- [ ] 确认返回的 JSON 包含全部 10 个字段
- [ ] 确认 `业务类别占比` 每项只有一个 key-value
- [ ] 确认 `职责对比` 每项都有 `name`、`actual`、`ideal`
- [ ] 访问 `http://localhost:8080/api/schedule/report/html`，确认浏览器预览正常
- [ ] 打开 `demo.html`，点击"下载 PDF 报表"，确认 PDF 能正常生成
- [ ] 检查 PDF 中两个图表是否正常渲染（环形图 + 折线图）
- [ ] 对比浏览器预览和 PDF，确认图表布局一致
- [ ] 检查中文是否正常显示，无乱码
